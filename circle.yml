machine:
  node:
    version: 7.5.0
  environment:
    NODE_ENV: test

dependencies:
  pre:
    - ./scripts/setupNpm.sh

test:
  override:
    - echo "done"

deployment:
  all:
    branch: /.*/
    commands:
      - ./scripts/release.sh
